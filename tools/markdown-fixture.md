# 見出し 1

段落です。同じ段落の 2 行目は改行として残ります。

空行をはさむと別の段落になります。**太字**、*斜体*、`インラインコード`、
[リンク](https://learn.microsoft.com/azure/devops/) を含みます。

## 見出し 2

- 箇条書き
- 入れ子の親
  - 子の項目
  - `コード` を含む子
- 親に戻る

1. 番号付き
2. 二番目
   - 番号付きの下の箇条書き
3. 三番目

### 見出し 3

| 項目 | 値 | 備考 |
| --- | --- | --- |
| 上限 | **100000** 行 | 強調が効く |
| 形式 | `text/csv` | コードが効く |

> 引用です。
> 2 行目も引用に入ります。

```csharp
if (rows > Max)
{
    throw new InvalidOperationException("too many rows");
}
```

---

エスケープの確認: `a < b && c > d` と、生の < と & と > 。

~~打消し~~ は未対応なので、記号がそのまま残ります。
